<section>

     <h3>Movie</h3>
     <div class="product-list">
          {{#each filteredProduct}}
          <div class="products">
               <a href="/cart/{{this.picName}}"><div class="movieTitle">{{this.title}}</div>
                    <img src="/uploads/{{this.sPic}}" alt="pic">
               </a>
          </div>

          {{/each}}
    </div>
</section>