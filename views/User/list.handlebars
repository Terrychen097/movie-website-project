<section id="list-section">

    {{#each filteredUsers}}
    <p>
        <a href="/users/{{this.id}}">Edit</a>
        <a href="/users/{{this.id}}?method=delete">Delete</a>

    </p>
    
    <a href="/users/{{this.id}}"><p>Name : {{this.name}}</p></a>
    <p>Email : {{this.email}}</p>

    {{/each}}



</section>